<Window x:Class="db_client.QueryWindows.AddField"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:db_client.QueryWindows"
        mc:Ignorable="d"
        Title="Add Field" Width="300" Height="210">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Name:" Margin="0,0,0,5"/>
            <TextBox x:Name="NameTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="Type:" Margin="0,0,0,5"/>
            <ComboBox x:Name="TypeComboBox" SelectedItem="{Binding SelectedEnumValue}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="Ok" Content="OK" Width="80" Margin="0,0,10,0"
                Click="Ok_Click"/>
            <Button x:Name="Cancel" Content="Cancel" Width="80"
                Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
