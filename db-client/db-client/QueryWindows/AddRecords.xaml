<Window x:Class="db_client.QueryWindows.AddRecords"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:db_client.QueryWindows"
        mc:Ignorable="d"
        Title="AddRecords" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Add New Records" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!--<DataGrid x:Name="RecordsDataGrid" Grid.Row="1" AutoGenerateColumns="True" 
                  CanUserAddRows="True" CanUserDeleteRows="True" Margin="0,0,0,10"/>-->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="FieldsPanel" />
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="AddButton" Click="AddButton_Click" Content="Add Records" Width="100" Height="30" Margin="5"/>
            <Button x:Name="CancelButton" Click="CancelButton_Click" Content="Cancel" Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
